# Extra dependencies for the delta-points bot (hedgebot) ONLY. Installed at runtime by
# scripts/run_both.sh when DELTA_BOT_ENABLED=true — kept out of pyproject.toml so a failure here can
# never block the copy bot's build/redeploy. python-telegram-bot, asyncpg, cryptography, aiohttp,
# python-dotenv already come from the main install.
hyperliquid-python-sdk>=0.19.0
# Bare git URL (NOT "lighter @ git+…"): the repo's real dist name is `lighter-sdk`, and naming it
# `lighter` made pip fall back to an unrelated PyPI `lighter` pinned to Python 3.10.10 and fail.
# Import name stays `lighter`. Requires Python ≥3.8, so fine on the 3.14 worker.
git+https://github.com/elliottech/lighter-python.git
eth-account>=0.13.0
